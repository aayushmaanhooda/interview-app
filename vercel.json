{
  "version": 2,
  "installCommand": "cd app && npm ci",
  "buildCommand": "cd app && CI=false npm run build",
  "outputDirectory": "app/build",
  "framework": null,
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
